import { modelsConn } from '../utils/conn.js'
const { template } = await modelsConn('template')

/**
 * 查询模板 
 */
const fintUser = async (params) => {
  return null
}

/**
 * 更新模板 
 */
const updateUser = async (params) => {
  return null
}

/**
 * 添加模板
 */
const addUser = async (params) => {
  return null
}