/**
 * 查询用户 
 */
const fintUser = control((params) => { 

})

/**
 * 更新用户 
 */
const updateUser = control('post', (params) => { 

})

/**
 * 添加用户 
 */
const addUser = control('post', (params) => { 

})